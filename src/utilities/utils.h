#ifndef UTILIS_H
#define UTILIS_H

#include <string.h>
#include <stdlib.h>
#include <stdio.h>

const char* const_strcat(const char* str1, const char* str2);
const char* float_to_string(float value);

#endif
